[package]
name = "hermes-protocol"
version = "0.1.0"
edition = "2021"
description = "Shared serial/WiFi protocol framing and message types for Hermes"

[lib]
name = "hermes_protocol"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[package]
name = "hermes-math"
version = "0.1.0"
edition = "2021"

[dependencies]
# We will use `alloc` for dynamic ASTs, but try to keep it no_std compatible otherwise.
# For parsing, we might use `nom` later, but for now let's keep it simple or add it if needed.

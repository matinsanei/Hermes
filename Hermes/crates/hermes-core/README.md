# hermes-core

Core traits, state management, and lightweight utils used across Hermes projects. This crate is intended to be hardware/platform-agnostic.

Example: a `StateManager` trait used by simulators and embedded targets.
